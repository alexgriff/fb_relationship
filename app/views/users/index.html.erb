<h1>All Users </h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <p><%= user.username %></p>
      <%= link_to "Add Friend", "/friendships/#{user.id}", method: "post" %>
    </li>
  <% end %>
</ul>
